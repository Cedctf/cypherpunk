[package]
name = "subscription_program"
version = "0.1.0"
description = "Simple subscription program with vault PDA"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "subscription_program"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.29.0"

